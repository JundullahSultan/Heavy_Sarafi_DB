PORT=5000
MONGODB_URI=mongodb://localhost:27017/heavy_sarafi

# Firebase Config (Optional in Dev - Mock Auth fallback will run if file is missing)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Cloudinary Config (Optional in Dev - will use default placeholder images if missing)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
